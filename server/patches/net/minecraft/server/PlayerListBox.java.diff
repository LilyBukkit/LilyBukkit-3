--- PlayerListBox.java
+++ PlayerListBox.java
@@ -1,6 +1,5 @@
-package net.minecraft.src;
+package net.minecraft.server;
 
 import java.util.Vector;
-import net.minecraft.server.MinecraftServer;
 import javax.swing.JList;
 
@@ -8,15 +7,15 @@
     private MinecraftServer mcServer;
     private int playerCount;
-    
+
     public PlayerListBox(final MinecraftServer minecraftServer) {
         this.playerCount = 0;
         (this.mcServer = minecraftServer).addToOnlinePlayerList(this);
     }
-    
+
     public void addAllPlayers() {
         if (this.playerCount++ % 20 == 0) {
             final Vector vector1 = new Vector();
             for (int i2 = 0; i2 < this.mcServer.configManager.playerEntities.size(); ++i2) {
-                vector1.add(((EntityPlayerMP)this.mcServer.configManager.playerEntities.get(i2)).username);
+                vector1.add(this.mcServer.configManager.playerEntities.get(i2).username);
             }
             this.setListData(vector1);
