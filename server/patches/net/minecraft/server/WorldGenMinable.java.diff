--- WorldGenMinable.java
+++ WorldGenMinable.java
@@ -1,3 +1,3 @@
-package net.minecraft.src;
+package net.minecraft.server;
 
 import java.util.Random;
@@ -6,10 +6,10 @@
     private int minableBlockId;
     private int numberOfBlocks;
-    
+
     public WorldGenMinable(final int minableBlockID, final int numberOfBlocks) {
         this.minableBlockId = minableBlockID;
         this.numberOfBlocks = numberOfBlocks;
     }
-    
+
     @Override
     public boolean generate(final World world, final Random rand, final int x, final int y, final int z) {
@@ -34,5 +34,5 @@
                         final double d20 = (i21 + 0.5 - d14) / (d18 / 2.0);
                         final double d21 = (i22 + 0.5 - d15) / (d17 / 2.0);
-                        if (d19 * d19 + d20 * d20 + d21 * d21 < 1.0 && world.getBlockId(i20, i21, i22) == Block.stone.blockID) {
+                        if (d19 * d19 + d20 * d20 + d21 * d21 < 1.0 && world.getBlockId(i20, i21, i22) == Block.STONE.blockID) {
                             world.setBlock(i20, i21, i22, this.minableBlockId);
                         }
