--- Pathfinder.java
+++ Pathfinder.java
@@ -1,3 +1,3 @@
-package net.minecraft.src;
+package net.minecraft.server;
 
 public class Pathfinder {
@@ -6,5 +6,5 @@
     private MCHashTable pointMap;
     private PathPoint[] pathOptions;
-    
+
     public Pathfinder(final IBlockAccess worldMap) {
         this.path = new Path();
@@ -13,13 +13,13 @@
         this.worldMap = worldMap;
     }
-    
+
     public PathEntity createEntityPathTo(final Entity entity1, final Entity entity2, final float f3) {
         return this.createEntityPathTo(entity1, entity2.posX, entity2.boundingBox.minY, entity2.posZ, f3);
     }
-    
+
     public PathEntity createEntityPathTo(final Entity entity, final int x, final int boundingBoxY, final int z, final float f5) {
         return this.createEntityPathTo(entity, x + 0.5f, boundingBoxY + 0.5f, z + 0.5f, f5);
     }
-    
+
     private PathEntity createEntityPathTo(final Entity entity, final double x, final double boundingBoxY, final double z, final float f8) {
         this.path.clearPath();
@@ -28,8 +28,7 @@
         final PathPoint pathPoint10 = this.openPoint(MathHelper.floor_double(x - entity.width / 2.0f), MathHelper.floor_double(boundingBoxY), MathHelper.floor_double(z - entity.width / 2.0f));
         final PathPoint pathPoint11 = new PathPoint(MathHelper.floor_float(entity.width + 1.0f), MathHelper.floor_float(entity.height + 1.0f), MathHelper.floor_float(entity.width + 1.0f));
-        final PathEntity pathEntity12 = this.addToPath(entity, pathPoint9, pathPoint10, pathPoint11, f8);
-        return pathEntity12;
+        return this.addToPath(entity, pathPoint9, pathPoint10, pathPoint11, f8);
     }
-    
+
     private PathEntity addToPath(final Entity entity, final PathPoint pathPoint1, final PathPoint pathPoint2, final PathPoint pathPoint3, final float f5) {
         pathPoint1.totalPathDistance = 0.0f;
@@ -70,5 +69,5 @@
         return this.createEntityPath(pathPoint1, pathPoint4);
     }
-    
+
     private int findPathOptions(final Entity entity, final PathPoint pathPoint1, final PathPoint pathPoint2, final PathPoint pathPoint3, final float f5) {
         int i6 = 0;
@@ -95,5 +94,5 @@
         return i6;
     }
-    
+
     private PathPoint getSafePoint(final Entity entity, final int i2, int i3, final int i4, final PathPoint pathPoint, final int i6) {
         PathPoint pathPoint2 = null;
@@ -124,6 +123,6 @@
         return pathPoint2;
     }
-    
-    private final PathPoint openPoint(final int x, final int y, final int z) {
+
+    private PathPoint openPoint(final int x, final int y, final int z) {
         final int i4 = x | y << 10 | z << 20;
         PathPoint pathPoint5 = (PathPoint)this.pointMap.lookup(i4);
@@ -134,5 +133,5 @@
         return pathPoint5;
     }
-    
+
     private int getVerticalOffset(final Entity entity, final int x, final int y, final int z, final PathPoint pathPoint) {
         for (int i6 = x; i6 < x + pathPoint.xCoord; ++i6) {
@@ -151,5 +150,5 @@
         return 1;
     }
-    
+
     private PathEntity createEntityPath(final PathPoint pathPoint1, final PathPoint pathPoint2) {
         int i3 = 1;
